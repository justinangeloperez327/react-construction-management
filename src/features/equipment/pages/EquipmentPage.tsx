import { AlertTriangle,Plus,Settings,Truck } from "lucide-react";import { useState } from "react";import { useOutletContext } from "react-router-dom";import { PageHeader } from "@/components/layout";import { Button,ConfirmationDialog,Dialog,ErrorState,MetricCard,Skeleton } from "@/components/ui";import type { Project } from "@/features/projects/types/project";import { EquipmentForm } from "@/features/equipment/components/EquipmentForm";import { EquipmentTable } from "@/features/equipment/components/EquipmentTable";import { useCreateEquipment,useDeleteEquipment,useEquipment } from "@/features/equipment/hooks/useEquipment";export function EquipmentPage(){const {project}=useOutletContext<{project:Project}>();const query=useEquipment(project.id);const create=useCreateEquipment(project.id);const remove=useDeleteEquipment(project.id);const [adding,setAdding]=useState(false);const [deleting,setDeleting]=useState<string>();if(query.isLoading)return <div className="table-loading"><Skeleton/><Skeleton/><Skeleton/></div>;if(query.isError)return <ErrorState title="Unable to load equipment" description="Project equipment records could not be retrieved." onRetry={()=>void query.refetch()}/>;const data=query.data??[];const serviceDue=data.filter(x=>x.nextServiceDate&&x.nextServiceDate<="2026-09-30"&&x.status!=="off-hire").length;return <><PageHeader eyebrow={project.projectNumber} title="Equipment" description="Manage project plant, equipment deployment, operating status and maintenance visibility." actions={<Button onClick={()=>setAdding(true)}><Plus size={16}/>Add equipment</Button>}/><section className="metrics"><MetricCard label="Registered equipment" value={data.length} detail="Project plant register" icon={<Truck size={19}/>}/><MetricCard label="In use" value={data.filter(x=>x.status==="in-use").length} detail="Currently deployed"/><MetricCard label="Maintenance / breakdown" value={data.filter(x=>x.status==="under-maintenance"||x.status==="breakdown").length} detail="Unavailable for operations" icon={<Settings size={19}/>}/><MetricCard label="Service due" value={serviceDue} detail="Due through Sep 30" icon={<AlertTriangle size={19}/>}/></section><section className="card"><EquipmentTable items={data} onDelete={setDeleting}/></section><Dialog open={adding} title="Add equipment" description="Register plant or equipment deployed to this project." onClose={()=>setAdding(false)}><EquipmentForm onCancel={()=>setAdding(false)} onSubmit={async values=>{await create.mutateAsync({...values,projectId:project.id});setAdding(false)}}/></Dialog><ConfirmationDialog open={!!deleting} title="Delete equipment?" description="This removes the selected equipment from the project register." confirmLabel="Delete equipment" danger onClose={()=>setDeleting(undefined)} onConfirm={async()=>{if(deleting)await remove.mutateAsync(deleting);setDeleting(undefined)}}/></>}
