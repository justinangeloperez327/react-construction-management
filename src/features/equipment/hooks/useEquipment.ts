import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { equipmentRepository } from "@/features/equipment/api";import type { EquipmentInput } from "@/features/equipment/types/equipment";const key=(projectId:string)=>["projects",projectId,"equipment"] as const;export function useEquipment(projectId:string){return useQuery({queryKey:key(projectId),queryFn:()=>equipmentRepository.getByProject(projectId),enabled:!!projectId})}export function useCreateEquipment(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(input:EquipmentInput)=>equipmentRepository.create(input),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}export function useDeleteEquipment(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(id:string)=>equipmentRepository.delete(id),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}
