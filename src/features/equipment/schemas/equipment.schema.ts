import { z } from "zod";export const equipmentSchema=z.object({equipmentNumber:z.string().trim().min(1,"Equipment number is required"),name:z.string().trim().min(2,"Equipment name is required"),category:z.string().trim().min(2,"Category is required"),ownership:z.enum(["owned","rented","subcontractor"]),provider:z.string().trim().min(2,"Provider is required"),status:z.enum(["available","in-use","idle","under-maintenance","breakdown","off-hire"]),location:z.string().trim().min(2,"Location is required"),operator:z.string().optional(),meterHours:z.coerce.number().min(0),lastServiceDate:z.string().optional(),nextServiceDate:z.string().optional(),remarks:z.string().optional()});export type EquipmentFormValues=z.infer<typeof equipmentSchema>;
