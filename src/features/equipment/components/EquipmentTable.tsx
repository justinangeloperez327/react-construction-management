import { Trash2 } from "lucide-react";import type { ColumnDef } from "@tanstack/react-table";import { DataTable } from "@/components/data-table";import { Button,StatusBadge } from "@/components/ui";import { getStatusTone } from "@/design/status";import type { EquipmentEntry } from "@/features/equipment/types/equipment";import { humanize } from "@/shared/utils";export function EquipmentTable({items,onDelete}:{items:EquipmentEntry[];onDelete:(id:string)=>void}){const columns:ColumnDef<EquipmentEntry,unknown>[]=[{accessorKey:"equipmentNumber",header:"Equipment",cell:({row})=><div><strong>{row.original.equipmentNumber}</strong><div className="muted">{row.original.name}</div></div>},{accessorKey:"category",header:"Category"},{accessorKey:"provider",header:"Provider"},{accessorKey:"location",header:"Location"},{accessorKey:"status",header:"Status",cell:({row})=><StatusBadge tone={getStatusTone(row.original.status)}>{humanize(row.original.status)}</StatusBadge>},{accessorKey:"meterHours",header:"Meter hours"},{accessorKey:"nextServiceDate",header:"Next service",cell:({row})=>row.original.nextServiceDate||"—"},{id:"actions",header:"",enableSorting:false,cell:({row})=><Button variant="ghost" aria-label={`Delete ${row.original.name}`} onClick={()=>onDelete(row.original.id)}><Trash2 size={15}/></Button>}];return <DataTable data={items} columns={columns} searchPlaceholder="Search equipment..." emptyTitle="No equipment registered" emptyDescription="Add plant and equipment deployed to this project."/>}
