import { z } from "zod";export const rfiSchema=z.object({rfiNumber:z.string().trim().min(1,"RFI number is required"),subject:z.string().trim().min(2,"Subject is required"),question:z.string().trim().min(5,"Question is required"),discipline:z.string().trim().min(2,"Discipline is required"),priority:z.enum(["normal","high","critical"]),status:z.enum(["draft","open","answered","closed","cancelled"]),raisedBy:z.string().trim().min(2),assignedTo:z.string().trim().min(2),raisedDate:z.string().min(1),responseDueDate:z.string().min(1),answeredDate:z.string().optional(),closedDate:z.string().optional(),response:z.string().optional(),wbsId:z.string().optional(),drawingId:z.string().optional(),documentId:z.string().optional(),costImpact:z.boolean(),scheduleImpact:z.boolean(),remarks:z.string().optional()});export type RfiFormValues=z.infer<typeof rfiSchema>;
