import { Trash2 } from "lucide-react";import type { ColumnDef } from "@tanstack/react-table";import { DataTable } from "@/components/data-table";import { Button,StatusBadge } from "@/components/ui";import { getStatusTone } from "@/design/status";import type { Rfi } from "@/features/rfis/types/rfi";import { humanize } from "@/shared/utils";export function RfisTable({items,onDelete}:{items:Rfi[];onDelete:(id:string)=>void}){const columns:ColumnDef<Rfi,unknown>[]=[{accessorKey:"rfiNumber",header:"RFI",cell:({row})=><div><strong>{row.original.rfiNumber}</strong><div className="muted">{row.original.subject}</div></div>},{accessorKey:"discipline",header:"Discipline"},{accessorKey:"assignedTo",header:"Assigned to"},{accessorKey:"priority",header:"Priority",cell:({row})=><StatusBadge tone={getStatusTone(row.original.priority)}>{humanize(row.original.priority)}</StatusBadge>},{accessorKey:"status",header:"Status",cell:({row})=><StatusBadge tone={getStatusTone(row.original.status)}>{humanize(row.original.status)}</StatusBadge>},{accessorKey:"responseDueDate",header:"Response due"},{id:"impact",header:"Impact",cell:({row})=>[row.original.costImpact?"Cost":null,row.original.scheduleImpact?"Schedule":null].filter(Boolean).join(" + ")||"None"},{id:"actions",header:"",enableSorting:false,cell:({row})=><Button variant="ghost" aria-label={"Delete "+row.original.rfiNumber} onClick={()=>onDelete(row.original.id)}><Trash2 size={15}/></Button>}];return <DataTable data={items} columns={columns} searchPlaceholder="Search RFIs..." emptyTitle="No RFIs" emptyDescription="Create the first request for information."/>}