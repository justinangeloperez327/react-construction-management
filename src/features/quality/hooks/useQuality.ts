import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { qualityRepository } from "@/features/quality/api";import type { QualityRecordInput } from "@/features/quality/types/quality";const key=(projectId:string)=>["projects",projectId,"quality"] as const;export function useQuality(projectId:string){return useQuery({queryKey:key(projectId),queryFn:()=>qualityRepository.getByProject(projectId),enabled:!!projectId})}export function useCreateQualityRecord(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(input:QualityRecordInput)=>qualityRepository.create(input),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}export function useDeleteQualityRecord(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(id:string)=>qualityRepository.delete(id),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}
