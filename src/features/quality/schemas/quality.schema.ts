import { z } from "zod";export const qualitySchema=z.object({recordNumber:z.string().trim().min(1,"Record number is required"),recordType:z.enum(["ncr","observation"]),title:z.string().trim().min(2),description:z.string().trim().min(5),discipline:z.string().trim().min(2),severity:z.enum(["minor","major","critical"]),status:z.enum(["open","corrective-action","verification","closed","cancelled"]),inspectionId:z.string().optional(),wbsId:z.string().optional(),drawingId:z.string().optional(),raisedBy:z.string().trim().min(2),responsibleParty:z.string().trim().min(2),raisedDate:z.string().min(1),dueDate:z.string().min(1),rootCause:z.string().optional(),correctiveAction:z.string().optional(),correctiveActionDate:z.string().optional(),verifiedBy:z.string().optional(),verificationDate:z.string().optional(),closureRemarks:z.string().optional(),closedDate:z.string().optional()});export type QualityFormValues=z.infer<typeof qualitySchema>;
