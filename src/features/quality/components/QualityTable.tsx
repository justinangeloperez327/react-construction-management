import { Trash2 } from "lucide-react";import type { ColumnDef } from "@tanstack/react-table";import { DataTable } from "@/components/data-table";import { Button,StatusBadge } from "@/components/ui";import { getStatusTone } from "@/design/status";import type { QualityRecord } from "@/features/quality/types/quality";import { humanize } from "@/shared/utils";export function QualityTable({items,onDelete}:{items:QualityRecord[];onDelete:(id:string)=>void}){const columns:ColumnDef<QualityRecord,unknown>[]=[{accessorKey:"recordNumber",header:"Record",cell:({row})=><div><strong>{row.original.recordNumber}</strong><div className="muted">{row.original.title}</div></div>},{accessorKey:"recordType",header:"Type",cell:({row})=>row.original.recordType==="ncr"?"NCR":"Observation"},{accessorKey:"discipline",header:"Discipline"},{accessorKey:"responsibleParty",header:"Responsible"},{accessorKey:"severity",header:"Severity",cell:({row})=><StatusBadge tone={getStatusTone(row.original.severity)}>{humanize(row.original.severity)}</StatusBadge>},{accessorKey:"status",header:"Status",cell:({row})=><StatusBadge tone={getStatusTone(row.original.status)}>{humanize(row.original.status)}</StatusBadge>},{accessorKey:"dueDate",header:"Due date"},{id:"actions",header:"",enableSorting:false,cell:({row})=><Button variant="ghost" aria-label={"Delete "+row.original.recordNumber} onClick={()=>onDelete(row.original.id)}><Trash2 size={15}/></Button>}];return <DataTable data={items} columns={columns} searchPlaceholder="Search quality records..." emptyTitle="No quality records" emptyDescription="Create the first NCR or quality observation."/>}