import { Plus } from "lucide-react";import { useState } from "react";import { useOutletContext } from "react-router-dom";import { PageHeader } from "@/components/layout";import { Button,ConfirmationDialog,Dialog,ErrorState,MetricCard,Skeleton } from "@/components/ui";import type { Project } from "@/features/projects/types/project";import { DailyProgressForm } from "@/features/daily-progress/components/DailyProgressForm";import { DailyProgressList } from "@/features/daily-progress/components/DailyProgressList";import { useCreateDailyProgress,useDailyProgress,useDeleteDailyProgress } from "@/features/daily-progress/hooks/useDailyProgress";export function DailyProgressPage(){const {project}=useOutletContext<{project:Project}>();const reports=useDailyProgress(project.id);const create=useCreateDailyProgress(project.id);const remove=useDeleteDailyProgress(project.id);const [adding,setAdding]=useState(false);const [deleting,setDeleting]=useState<string>();if(reports.isLoading)return <div className="table-loading"><Skeleton/><Skeleton/><Skeleton/></div>;if(reports.isError)return <ErrorState title="Unable to load daily progress" description="Site progress reports could not be retrieved." onRetry={()=>void reports.refetch()}/>;const data=reports.data??[];const latest=data[0];return <><PageHeader eyebrow={project.projectNumber} title="Daily Progress" description="Capture actual site conditions, resources, constraints and daily execution progress." actions={<Button onClick={()=>setAdding(true)}><Plus size={16}/>New daily report</Button>}/><section className="metrics"><MetricCard label="Reports" value={data.length} detail="Project daily records"/><MetricCard label="Latest manpower" value={latest?.manpowerTotal??0} detail={latest?.reportDate??"No report"}/><MetricCard label="Equipment in use" value={latest?.equipmentTotal??0} detail="Latest report"/><MetricCard label="Submitted" value={data.filter(x=>x.status==="submitted").length} detail="Formal daily records"/></section><DailyProgressList reports={data} onDelete={setDeleting}/><Dialog open={adding} title="New daily progress report" description="Record the site's actual conditions and work completed today." onClose={()=>setAdding(false)}><DailyProgressForm onCancel={()=>setAdding(false)} onSubmit={async values=>{await create.mutateAsync({...values,projectId:project.id,entries:[]});setAdding(false)}}/></Dialog><ConfirmationDialog open={!!deleting} title="Delete daily report?" description="This removes the daily site record from the project." confirmLabel="Delete report" danger onClose={()=>setDeleting(undefined)} onConfirm={async()=>{if(deleting)await remove.mutateAsync(deleting);setDeleting(undefined)}}/></>}
