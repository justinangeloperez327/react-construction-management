import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { dailyProgressRepository } from "@/features/daily-progress/api";import type { DailyProgressInput } from "@/features/daily-progress/types/dailyProgress";const key=(projectId:string)=>["projects",projectId,"daily-progress"] as const;export function useDailyProgress(projectId:string){return useQuery({queryKey:key(projectId),queryFn:()=>dailyProgressRepository.getByProject(projectId),enabled:!!projectId})}export function useCreateDailyProgress(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(input:DailyProgressInput)=>dailyProgressRepository.create(input),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}export function useDeleteDailyProgress(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(id:string)=>dailyProgressRepository.delete(id),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}
