import { Trash2 } from "lucide-react";import type { ColumnDef } from "@tanstack/react-table";import { DataTable } from "@/components/data-table";import { Button } from "@/components/ui";import type { ManpowerEntry } from "@/features/manpower/types/manpower";import { humanize } from "@/shared/utils";export function ManpowerTable({entries,onDelete}:{entries:ManpowerEntry[];onDelete:(id:string)=>void}){const columns:ColumnDef<ManpowerEntry,unknown>[]=[{accessorKey:"date",header:"Date"},{accessorKey:"contractor",header:"Contractor"},{accessorKey:"trade",header:"Trade / Role"},{accessorKey:"category",header:"Category",cell:({row})=>humanize(row.original.category)},{accessorKey:"headcount",header:"Headcount"},{id:"hours",header:"Hours",cell:({row})=><span>{row.original.regularHours}h + {row.original.overtimeHours}h OT</span>},{id:"manhours",header:"Man-hours",cell:({row})=><strong>{row.original.headcount*(row.original.regularHours+row.original.overtimeHours)}</strong>},{id:"actions",header:"",enableSorting:false,cell:({row})=><Button variant="ghost" aria-label={`Delete ${row.original.trade}`} onClick={()=>onDelete(row.original.id)}><Trash2 size={15}/></Button>}];return <DataTable data={entries} columns={columns} searchPlaceholder="Search contractor or trade..." emptyTitle="No manpower records" emptyDescription="Add workforce deployed to this project."/>}
