import { z } from "zod";export const manpowerSchema=z.object({date:z.string().min(1,"Date is required"),contractor:z.string().trim().min(2,"Contractor is required"),trade:z.string().trim().min(2,"Trade is required"),category:z.enum(["management","engineering","supervision","skilled","unskilled","specialist"]),headcount:z.coerce.number().int().min(1),regularHours:z.coerce.number().min(0).max(24),overtimeHours:z.coerce.number().min(0).max(24),remarks:z.string().optional()});export type ManpowerFormValues=z.infer<typeof manpowerSchema>;
