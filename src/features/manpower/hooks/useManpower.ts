import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { manpowerRepository } from "@/features/manpower/api";import type { ManpowerInput } from "@/features/manpower/types/manpower";const key=(projectId:string)=>["projects",projectId,"manpower"] as const;export function useManpower(projectId:string){return useQuery({queryKey:key(projectId),queryFn:()=>manpowerRepository.getByProject(projectId),enabled:!!projectId})}export function useCreateManpower(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(input:ManpowerInput)=>manpowerRepository.create(input),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}export function useDeleteManpower(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(id:string)=>manpowerRepository.delete(id),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}
