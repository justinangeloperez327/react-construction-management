import { Link } from "react-router-dom";import type { ColumnDef } from "@tanstack/react-table";import { DataTable } from "@/components/data-table";import { StatusBadge } from "@/components/ui";import { getStatusTone } from "@/design/status";import type { ProjectSearchResult } from "@/features/search/types/search";import { humanize } from "@/shared/utils";export function SearchResultsTable({items,projectId}:{items:ProjectSearchResult[];projectId:string}){const columns:ColumnDef<ProjectSearchResult,unknown>[]=[{accessorKey:"reference",header:"Reference",cell:({row})=><Link to={"/projects/"+projectId+"/"+row.original.path}><strong>{row.original.reference}</strong></Link>},{accessorKey:"entityType",header:"Type",cell:({row})=>humanize(row.original.entityType)},{accessorKey:"title",header:"Title",cell:({row})=><div><strong>{row.original.title}</strong><div className="muted">{row.original.description}</div></div>},{accessorKey:"discipline",header:"Discipline",cell:({row})=>row.original.discipline||"—"},{accessorKey:"owner",header:"Owner",cell:({row})=>row.original.owner||"—"},{accessorKey:"status",header:"Status",cell:({row})=>row.original.status?<StatusBadge tone={getStatusTone(row.original.status)}>{humanize(row.original.status)}</StatusBadge>:"—"},{accessorKey:"date",header:"Relevant date",cell:({row})=>row.original.date||"—"}];return <DataTable data={items} columns={columns} searchPlaceholder="Search reference, title, description, owner..." emptyTitle="No matching records" emptyDescription="Adjust the search or filters." pageSize={15}/>}