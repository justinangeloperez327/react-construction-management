import { z } from "zod";export const safetySchema=z.object({recordNumber:z.string().trim().min(1),recordType:z.enum(["observation","near-miss","incident","toolbox-talk","inspection"]),title:z.string().trim().min(2),description:z.string().trim().min(5),severity:z.enum(["low","medium","high","critical"]),status:z.enum(["open","action-required","under-investigation","verification","closed","cancelled"]),location:z.string().trim().min(2),wbsId:z.string().optional(),activityId:z.string().optional(),reportedBy:z.string().trim().min(2),responsibleParty:z.string().optional(),reportedDate:z.string().min(1),dueDate:z.string().optional(),incidentDate:z.string().optional(),immediateAction:z.string().optional(),rootCause:z.string().optional(),correctiveAction:z.string().optional(),verifiedBy:z.string().optional(),verificationDate:z.string().optional(),closedDate:z.string().optional(),lostTime:z.boolean(),medicalTreatment:z.boolean(),remarks:z.string().optional()});export type SafetyFormValues=z.infer<typeof safetySchema>;
