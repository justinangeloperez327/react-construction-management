import { AlertTriangle,CalendarCheck2,ClipboardCheck,Plus } from "lucide-react";import { useState } from "react";import { useOutletContext } from "react-router-dom";import { PageHeader } from "@/components/layout";import { Button,ConfirmationDialog,Dialog,ErrorState,MetricCard,Skeleton } from "@/components/ui";import type { Project } from "@/features/projects/types/project";import { useWbs } from "@/features/wbs/hooks/useWbs";import { useActivities } from "@/features/activities/hooks/useActivities";import { useMaterials } from "@/features/materials/hooks/useMaterials";import { useDrawings } from "@/features/drawings/hooks/useDrawings";import { InspectionForm } from "@/features/inspections/components/InspectionForm";import { InspectionsTable } from "@/features/inspections/components/InspectionsTable";import { useCreateInspection,useDeleteInspection,useInspections } from "@/features/inspections/hooks/useInspections";export function InspectionsPage(){const {project}=useOutletContext<{project:Project}>();const query=useInspections(project.id),wbs=useWbs(project.id),activities=useActivities(project.id),materials=useMaterials(project.id),drawings=useDrawings(project.id);const create=useCreateInspection(project.id),remove=useDeleteInspection(project.id);const [adding,setAdding]=useState(false),[deleting,setDeleting]=useState<string>();if(query.isLoading||wbs.isLoading||activities.isLoading||materials.isLoading||drawings.isLoading)return <div className="table-loading"><Skeleton/><Skeleton/><Skeleton/></div>;if(query.isError||wbs.isError||activities.isError||materials.isError||drawings.isError)return <ErrorState title="Unable to load inspections" description="Inspection or linked project records could not be retrieved." onRetry={()=>void query.refetch()}/>;const data=query.data??[];const pending=data.filter(x=>["requested","scheduled"].includes(x.status));const passed=data.filter(x=>["passed","passed-with-comments"].includes(x.status));const failed=data.filter(x=>x.status==="failed");return <><PageHeader eyebrow={project.projectNumber} title="Inspections" description="Manage work and material inspection requests, results and quality follow-up." actions={<Button onClick={()=>setAdding(true)}><Plus size={16}/>New inspection</Button>}/><section className="metrics"><MetricCard label="Inspections" value={data.length} detail="Inspection register" icon={<ClipboardCheck size={19}/>}/><MetricCard label="Pending" value={pending.length} detail="Requested or scheduled" icon={<CalendarCheck2 size={19}/>}/><MetricCard label="Passed" value={passed.length} detail="Accepted inspections"/><MetricCard label="Failed" value={failed.length} detail="Requires attention" icon={<AlertTriangle size={19}/>}/></section><section className="card"><InspectionsTable items={data} onDelete={setDeleting}/></section><Dialog open={adding} title="New inspection" description="Register an inspection and link it to the relevant project records." onClose={()=>setAdding(false)}><InspectionForm wbsItems={wbs.data??[]} activities={activities.data??[]} materials={materials.data??[]} drawings={drawings.data??[]} onCancel={()=>setAdding(false)} onSubmit={async values=>{await create.mutateAsync({...values,projectId:project.id});setAdding(false)}}/></Dialog><ConfirmationDialog open={!!deleting} title="Delete inspection?" description="This removes the inspection from the project register." confirmLabel="Delete inspection" danger onClose={()=>setDeleting(undefined)} onConfirm={async()=>{if(deleting)await remove.mutateAsync(deleting);setDeleting(undefined)}}/></>}