import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { inspectionRepository } from "@/features/inspections/api";import type { InspectionInput } from "@/features/inspections/types/inspection";const key=(projectId:string)=>["projects",projectId,"inspections"] as const;export function useInspections(projectId:string){return useQuery({queryKey:key(projectId),queryFn:()=>inspectionRepository.getByProject(projectId),enabled:!!projectId})}export function useCreateInspection(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(input:InspectionInput)=>inspectionRepository.create(input),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}export function useDeleteInspection(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(id:string)=>inspectionRepository.delete(id),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}
