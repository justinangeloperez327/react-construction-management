import { z } from "zod";export const inspectionSchema=z.object({inspectionNumber:z.string().trim().min(1,"Inspection number is required"),title:z.string().trim().min(2,"Title is required"),inspectionType:z.enum(["work","material","mockup","testing","handover"]),discipline:z.string().trim().min(2),status:z.enum(["draft","requested","scheduled","passed","passed-with-comments","failed","cancelled"]),wbsId:z.string().optional(),activityId:z.string().optional(),materialId:z.string().optional(),drawingId:z.string().optional(),requestedBy:z.string().trim().min(2),inspector:z.string().trim().min(2),requestedDate:z.string().min(1),scheduledDate:z.string().optional(),inspectionDate:z.string().optional(),resultDate:z.string().optional(),location:z.string().trim().min(2),checklistReference:z.string().optional(),resultRemarks:z.string().optional(),requiresCorrectiveAction:z.boolean(),remarks:z.string().optional()});export type InspectionFormValues=z.infer<typeof inspectionSchema>;
