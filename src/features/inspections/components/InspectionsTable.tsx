import { Trash2 } from "lucide-react";import type { ColumnDef } from "@tanstack/react-table";import { DataTable } from "@/components/data-table";import { Button,StatusBadge } from "@/components/ui";import { getStatusTone } from "@/design/status";import type { Inspection } from "@/features/inspections/types/inspection";import { humanize } from "@/shared/utils";export function InspectionsTable({items,onDelete}:{items:Inspection[];onDelete:(id:string)=>void}){const columns:ColumnDef<Inspection,unknown>[]=[{accessorKey:"inspectionNumber",header:"Inspection",cell:({row})=><div><strong>{row.original.inspectionNumber}</strong><div className="muted">{row.original.title}</div></div>},{accessorKey:"inspectionType",header:"Type",cell:({row})=>humanize(row.original.inspectionType)},{accessorKey:"discipline",header:"Discipline"},{accessorKey:"location",header:"Location"},{accessorKey:"inspector",header:"Inspector"},{accessorKey:"scheduledDate",header:"Scheduled",cell:({row})=>row.original.scheduledDate||"—"},{accessorKey:"status",header:"Status",cell:({row})=><StatusBadge tone={getStatusTone(row.original.status)}>{humanize(row.original.status)}</StatusBadge>},{id:"followup",header:"Follow-up",cell:({row})=>row.original.requiresCorrectiveAction?<StatusBadge tone="danger">Corrective action</StatusBadge>:"—"},{id:"actions",header:"",enableSorting:false,cell:({row})=><Button variant="ghost" aria-label={"Delete "+row.original.inspectionNumber} onClick={()=>onDelete(row.original.id)}><Trash2 size={15}/></Button>}];return <DataTable data={items} columns={columns} searchPlaceholder="Search inspections..." emptyTitle="No inspections" emptyDescription="Create the first inspection request."/>}