import { z } from "zod";export const materialSchema=z.object({materialNumber:z.string().trim().min(1,"Material number is required"),name:z.string().trim().min(2,"Material name is required"),category:z.string().trim().min(2,"Category is required"),unit:z.string().trim().min(1,"Unit is required"),requiredQuantity:z.coerce.number().min(0),orderedQuantity:z.coerce.number().min(0),deliveredQuantity:z.coerce.number().min(0),consumedQuantity:z.coerce.number().min(0),supplier:z.string().optional(),requiredDate:z.string().optional(),lastDeliveryDate:z.string().optional(),inspectionStatus:z.enum(["not-required","pending","approved","rejected"]),status:z.enum(["required","ordered","partially-delivered","delivered","approved","rejected"]),storageLocation:z.string().optional(),remarks:z.string().optional()});export type MaterialFormValues=z.infer<typeof materialSchema>;
