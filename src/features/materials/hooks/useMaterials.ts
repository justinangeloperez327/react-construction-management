import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { materialRepository } from "@/features/materials/api";import type { MaterialInput } from "@/features/materials/types/material";const key=(projectId:string)=>["projects",projectId,"materials"] as const;export function useMaterials(projectId:string){return useQuery({queryKey:key(projectId),queryFn:()=>materialRepository.getByProject(projectId),enabled:!!projectId})}export function useCreateMaterial(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(input:MaterialInput)=>materialRepository.create(input),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}export function useDeleteMaterial(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(id:string)=>materialRepository.delete(id),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}
