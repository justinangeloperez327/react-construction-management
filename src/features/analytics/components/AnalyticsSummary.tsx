import { AlertTriangle,BarChart3,CircleDollarSign,Gauge } from "lucide-react";import { MetricCard } from "@/components/ui";export function AnalyticsSummary({metrics}:{metrics:{physicalProgress:number;lateActivities:number;budget:number;forecast:number;costVariance:number;approvedVariationCost:number;openRfis:number;openQuality:number;openSafety:number;openIssues:number}}){const money=(n:number)=>new Intl.NumberFormat("en-AE",{style:"currency",currency:"AED",notation:"compact",maximumFractionDigits:1}).format(n);return <section className="metrics"><MetricCard label="Physical progress" value={metrics.physicalProgress.toFixed(1)+"%"} detail="Average activity progress" icon={<Gauge size={19}/>}/><MetricCard label="Late activities" value={metrics.lateActivities} detail="Past planned finish" icon={<AlertTriangle size={19}/>}/><MetricCard label="Forecast at completion" value={money(metrics.forecast)} detail={"Budget "+money(metrics.budget)} icon={<CircleDollarSign size={19}/>}/><MetricCard label="Cost variance" value={money(metrics.costVariance)} detail="Budget less forecast" icon={<BarChart3 size={19}/>}/></section>}