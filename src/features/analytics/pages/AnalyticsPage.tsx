import { PageHeader } from "@/components/layout";import { ErrorState,Skeleton } from "@/components/ui";import { useOutletContext } from "react-router-dom";import type { Project } from "@/features/projects/types/project";import { useProjectAnalytics } from "@/features/analytics/hooks/useProjectAnalytics";import { AnalyticsSummary } from "@/features/analytics/components/AnalyticsSummary";import { AnalyticsBreakdown } from "@/features/analytics/components/AnalyticsBreakdown";export function AnalyticsPage(){const {project}=useOutletContext<{project:Project}>();const analytics=useProjectAnalytics(project.id);if(analytics.loading)return <div className="table-loading"><Skeleton/><Skeleton/><Skeleton/></div>;if(analytics.error)return <ErrorState title="Unable to calculate analytics" description="One or more source modules could not be retrieved." onRetry={()=>void analytics.refetch()}/>;return <><PageHeader eyebrow={project.projectNumber} title="Analytics" description="Cross-functional project performance derived from authoritative operational modules."/><AnalyticsSummary metrics={analytics.metrics}/><AnalyticsBreakdown metrics={analytics.metrics}/></>}