import { z } from "zod";export const drawingSchema=z.object({documentId:z.string().optional(),drawingNumber:z.string().trim().min(1,"Drawing number is required"),title:z.string().trim().min(2,"Title is required"),discipline:z.string().trim().min(2,"Discipline is required"),drawingType:z.enum(["design","shop-drawing","coordination","as-built"]),revision:z.string().trim().min(1,"Revision is required"),status:z.enum(["draft","submitted","under-review","approved","approved-with-comments","rejected","superseded"]),originator:z.string().trim().min(2,"Originator is required"),zone:z.string().optional(),level:z.string().optional(),submittedDate:z.string().optional(),responseDueDate:z.string().optional(),responseDate:z.string().optional(),supersedesDrawingId:z.string().optional(),remarks:z.string().optional()});export type DrawingFormValues=z.infer<typeof drawingSchema>;
