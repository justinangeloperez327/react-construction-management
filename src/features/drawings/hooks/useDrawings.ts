import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { drawingRepository } from "@/features/drawings/api";import type { DrawingInput } from "@/features/drawings/types/drawing";const key=(projectId:string)=>["projects",projectId,"drawings"] as const;export function useDrawings(projectId:string){return useQuery({queryKey:key(projectId),queryFn:()=>drawingRepository.getByProject(projectId),enabled:!!projectId})}export function useCreateDrawing(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(input:DrawingInput)=>drawingRepository.create(input),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}export function useDeleteDrawing(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(id:string)=>drawingRepository.delete(id),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}
