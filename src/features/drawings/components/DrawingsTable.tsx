import { Trash2 } from "lucide-react";import type { ColumnDef } from "@tanstack/react-table";import { DataTable } from "@/components/data-table";import { Button,StatusBadge } from "@/components/ui";import { getStatusTone } from "@/design/status";import type { Drawing } from "@/features/drawings/types/drawing";import { humanize } from "@/shared/utils";export function DrawingsTable({items,onDelete}:{items:Drawing[];onDelete:(id:string)=>void}){const columns:ColumnDef<Drawing,unknown>[]=[{accessorKey:"drawingNumber",header:"Drawing",cell:({row})=><div><strong>{row.original.drawingNumber}</strong><div className="muted">{row.original.title}</div></div>},{accessorKey:"revision",header:"Rev."},{accessorKey:"drawingType",header:"Type",cell:({row})=>humanize(row.original.drawingType)},{accessorKey:"discipline",header:"Discipline"},{accessorKey:"zone",header:"Zone / Level",cell:({row})=>[row.original.zone,row.original.level].filter(Boolean).join(" · ")||"—"},{accessorKey:"status",header:"Status",cell:({row})=><StatusBadge tone={getStatusTone(row.original.status)}>{humanize(row.original.status)}</StatusBadge>},{accessorKey:"originator",header:"Originator"},{accessorKey:"responseDueDate",header:"Response due",cell:({row})=>row.original.responseDueDate||"—"},{id:"actions",header:"",enableSorting:false,cell:({row})=><Button variant="ghost" aria-label={"Delete "+row.original.drawingNumber} onClick={()=>onDelete(row.original.id)}><Trash2 size={15}/></Button>}];return <DataTable data={items} columns={columns} searchPlaceholder="Search drawing register..." emptyTitle="No drawings" emptyDescription="Register the first project drawing."/>}