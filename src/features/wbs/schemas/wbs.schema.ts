import { z } from "zod";
export const wbsFormSchema=z.object({code:z.string().trim().min(1,"WBS code is required"),name:z.string().trim().min(2,"Name is required"),description:z.string().optional(),parentId:z.string().optional(),discipline:z.enum(["architectural","civil","structural","mechanical","electrical","plumbing","fire-life-safety","landscape","interior","infrastructure","general"]),status:z.enum(["planned","in-progress","completed","on-hold"]),progress:z.coerce.number().min(0).max(100),weight:z.coerce.number().min(0).max(100)});export type WbsFormValues=z.infer<typeof wbsFormSchema>;
