import { zodResolver } from "@hookform/resolvers/zod";import { useForm } from "react-hook-form";import { Button,FormField,Input,Select,Textarea } from "@/components/ui";import { FormActions,FormSection } from "@/components/forms";import { disciplines } from "@/shared/constants";import { wbsFormSchema,type WbsFormValues } from "@/features/wbs/schemas/wbs.schema";import type { WbsItem } from "@/features/wbs/types/wbs";
export function WbsForm({items,onSubmit,onCancel}:{items:WbsItem[];onSubmit:(v:WbsFormValues)=>void|Promise<void>;onCancel:()=>void}){const {register,handleSubmit,formState:{errors,isSubmitting}}=useForm<WbsFormValues>({resolver:zodResolver(wbsFormSchema),defaultValues:{discipline:"general",status:"planned",progress:0,weight:0}});return <form onSubmit={handleSubmit(onSubmit)}><FormSection title="WBS item" description="Define a hierarchical scope element for planning and control."><FormField id="code" label="WBS code" required error={errors.code?.message}><Input id="code" {...register("code")} invalid={!!errors.code}/></FormField><FormField id="name" label="Name" required error={errors.name?.message}><Input id="name" {...register("name")} invalid={!!errors.name}/></FormField><FormField id="parentId" label="Parent item"><Select id="parentId" {...register("parentId")}><option value="">Top level</option>{items.map(x=><option key={x.id} value={x.id}>{x.code} — {x.name}</option>)}</Select></FormField><FormField id="discipline" label="Discipline" required><Select id="discipline" {...register("discipline")}>{disciplines.map(x=><option key={x.value} value={x.value}>{x.label}</option>)}</Select></FormField><FormField id="status" label="Status" required><Select id="status" {...register("status")}><option value="planned">Planned</option><option value="in-progress">In progress</option><option value="completed">Completed</option><option value="on-hold">On hold</option></Select></FormField><FormField id="progress" label="Progress (%)" error={errors.progress?.message}><Input id="progress" type="number" min="0" max="100" {...register("progress")}/></FormField><FormField id="weight" label="Weight (%)" error={errors.weight?.message}><Input id="weight" type="number" min="0" max="100" {...register("weight")}/></FormField><FormField id="description" label="Description"><Textarea id="description" {...register("description")}/></FormField></FormSection><FormActions><Button type="button" variant="secondary" onClick={onCancel}>Cancel</Button><Button type="submit" disabled={isSubmitting}>{isSubmitting?"Saving…":"Add WBS item"}</Button></FormActions></form>}
