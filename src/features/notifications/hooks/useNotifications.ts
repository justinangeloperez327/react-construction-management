import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { notificationRepository } from "@/features/notifications/api";const key=(projectId:string)=>["projects",projectId,"notifications"] as const;export function useNotifications(projectId:string){return useQuery({queryKey:key(projectId),queryFn:()=>notificationRepository.getByProject(projectId),enabled:!!projectId})}export function useNotificationActions(projectId:string){const client=useQueryClient(),invalidate=()=>client.invalidateQueries({queryKey:key(projectId)});const markRead=useMutation({mutationFn:(id:string)=>notificationRepository.markRead(id),onSuccess:invalidate});const markAllRead=useMutation({mutationFn:()=>notificationRepository.markAllRead(projectId),onSuccess:invalidate});const dismiss=useMutation({mutationFn:(id:string)=>notificationRepository.dismiss(id),onSuccess:invalidate});return {markRead,markAllRead,dismiss}}
