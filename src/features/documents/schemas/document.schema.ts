import { z } from "zod";export const documentSchema=z.object({documentNumber:z.string().trim().min(1,"Document number is required"),title:z.string().trim().min(2,"Title is required"),category:z.enum(["drawing","specification","method-statement","material-submittal","report","correspondence","contract","other"]),discipline:z.string().trim().min(2,"Discipline is required"),revision:z.string().trim().min(1,"Revision is required"),status:z.enum(["draft","submitted","under-review","approved","approved-with-comments","rejected","superseded"]),originator:z.string().trim().min(2,"Originator is required"),submittedDate:z.string().optional(),responseDueDate:z.string().optional(),responseDate:z.string().optional(),transmittalNumber:z.string().optional(),fileName:z.string().optional(),description:z.string().optional(),remarks:z.string().optional()});export type DocumentFormValues=z.infer<typeof documentSchema>;
