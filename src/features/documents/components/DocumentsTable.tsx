import { Trash2 } from "lucide-react";import type { ColumnDef } from "@tanstack/react-table";import { DataTable } from "@/components/data-table";import { Button,StatusBadge } from "@/components/ui";import { getStatusTone } from "@/design/status";import type { ProjectDocument } from "@/features/documents/types/document";import { humanize } from "@/shared/utils";export function DocumentsTable({items,onDelete}:{items:ProjectDocument[];onDelete:(id:string)=>void}){const columns:ColumnDef<ProjectDocument,unknown>[]=[{accessorKey:"documentNumber",header:"Document",cell:({row})=><div><strong>{row.original.documentNumber}</strong><div className="muted">{row.original.title}</div></div>},{accessorKey:"revision",header:"Rev."},{accessorKey:"category",header:"Category",cell:({row})=>humanize(row.original.category)},{accessorKey:"discipline",header:"Discipline"},{accessorKey:"originator",header:"Originator"},{accessorKey:"status",header:"Status",cell:({row})=><StatusBadge tone={getStatusTone(row.original.status)}>{humanize(row.original.status)}</StatusBadge>},{accessorKey:"responseDueDate",header:"Response due",cell:({row})=>row.original.responseDueDate||"—"},{accessorKey:"transmittalNumber",header:"Transmittal",cell:({row})=>row.original.transmittalNumber||"—"},{id:"actions",header:"",enableSorting:false,cell:({row})=><Button variant="ghost" aria-label={"Delete "+row.original.documentNumber} onClick={()=>onDelete(row.original.id)}><Trash2 size={15}/></Button>}];return <DataTable data={items} columns={columns} searchPlaceholder="Search document register..." emptyTitle="No documents" emptyDescription="Register the first controlled project document."/>}