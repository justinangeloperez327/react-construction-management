import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { issueRepository } from "@/features/issues/api";import type { ProjectIssueInput } from "@/features/issues/types/issue";const key=(projectId:string)=>["projects",projectId,"issues"] as const;export function useIssues(projectId:string){return useQuery({queryKey:key(projectId),queryFn:()=>issueRepository.getByProject(projectId),enabled:!!projectId})}export function useCreateIssue(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(input:ProjectIssueInput)=>issueRepository.create(input),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}export function useDeleteIssue(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(id:string)=>issueRepository.delete(id),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}
