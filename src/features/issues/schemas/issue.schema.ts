import { z } from "zod";export const issueSchema=z.object({issueNumber:z.string().trim().min(1),title:z.string().trim().min(2),description:z.string().trim().min(5),issueType:z.enum(["technical","quality","safety","commercial","schedule","coordination","other"]),priority:z.enum(["low","normal","high","critical"]),status:z.enum(["open","in-progress","blocked","resolved","closed","cancelled"]),raisedBy:z.string().trim().min(2),owner:z.string().trim().min(2),raisedDate:z.string().min(1),dueDate:z.string().optional(),resolvedDate:z.string().optional(),closedDate:z.string().optional(),location:z.string().optional(),wbsId:z.string().optional(),activityId:z.string().optional(),rfiId:z.string().optional(),inspectionId:z.string().optional(),qualityRecordId:z.string().optional(),safetyRecordId:z.string().optional(),costImpact:z.boolean(),scheduleImpact:z.boolean(),resolution:z.string().optional(),remarks:z.string().optional()});export type IssueFormValues=z.infer<typeof issueSchema>;
