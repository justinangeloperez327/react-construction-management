import { z } from "zod";export const activityFormSchema=z.object({wbsId:z.string().min(1,"WBS item is required"),activityNumber:z.string().trim().min(1,"Activity number is required"),name:z.string().trim().min(2,"Activity name is required"),description:z.string().optional(),discipline:z.enum(["architectural","civil","structural","mechanical","electrical","plumbing","fire-life-safety","landscape","interior","infrastructure","general"]),priority:z.enum(["low","normal","high","urgent","critical"]),status:z.enum(["not-started","in-progress","on-hold","completed","cancelled"]),plannedStart:z.string().min(1,"Start date is required"),plannedFinish:z.string().min(1,"Finish date is required"),progress:z.coerce.number().min(0).max(100),responsible:z.string().trim().min(2,"Responsible party is required")}).refine(x=>x.plannedFinish>=x.plannedStart,{message:"Finish date must be on or after start date",path:["plannedFinish"]});export type ActivityFormValues=z.infer<typeof activityFormSchema>;
