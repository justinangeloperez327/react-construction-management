import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { activityRepository } from "@/features/activities/api";import type { ActivityInput } from "@/features/activities/types/activity";const key=(projectId:string)=>["projects",projectId,"activities"] as const;export function useActivities(projectId:string){return useQuery({queryKey:key(projectId),queryFn:()=>activityRepository.getByProject(projectId),enabled:!!projectId})}export function useCreateActivity(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(input:ActivityInput)=>activityRepository.create(input),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}export function useDeleteActivity(projectId:string){const client=useQueryClient();return useMutation({mutationFn:(id:string)=>activityRepository.delete(id),onSuccess:async()=>client.invalidateQueries({queryKey:key(projectId)})})}
