import { Plus } from "lucide-react";import { useState } from "react";import { useOutletContext } from "react-router-dom";import { PageHeader } from "@/components/layout";import { Button,ConfirmationDialog,Dialog,ErrorState,Skeleton } from "@/components/ui";import type { Project } from "@/features/projects/types/project";import { ActivityForm } from "@/features/activities/components/ActivityForm";import { ActivitiesTable } from "@/features/activities/components/ActivitiesTable";import { useActivities,useCreateActivity,useDeleteActivity } from "@/features/activities/hooks/useActivities";import { useWbs } from "@/features/wbs/hooks/useWbs";
export function ActivitiesPage(){const {project}=useOutletContext<{project:Project}>();const activities=useActivities(project.id);const wbs=useWbs(project.id);const create=useCreateActivity(project.id);const remove=useDeleteActivity(project.id);const [adding,setAdding]=useState(false);const [deleting,setDeleting]=useState<string>();const loading=activities.isLoading||wbs.isLoading;const failed=activities.isError||wbs.isError;return <><PageHeader eyebrow={project.projectNumber} title="Activities" description="Plan and control execution activities against the project Work Breakdown Structure." actions={<Button onClick={()=>setAdding(true)} disabled={!wbs.data?.length}><Plus size={16}/>Add activity</Button>}/><section className="card">{loading?<div className="table-loading"><Skeleton/><Skeleton/><Skeleton/></div>:failed?<ErrorState title="Unable to load activities" description="Activities or WBS scope could not be retrieved." onRetry={()=>{void activities.refetch();void wbs.refetch()}}/>:<ActivitiesTable activities={activities.data??[]} wbsItems={wbs.data??[]} onDelete={setDeleting}/>}</section><Dialog open={adding} title="Add activity" description="Create an execution activity linked to a WBS scope item." onClose={()=>setAdding(false)}><ActivityForm wbsItems={wbs.data??[]} onCancel={()=>setAdding(false)} onSubmit={async values=>{await create.mutateAsync({...values,projectId:project.id});setAdding(false)}}/></Dialog><ConfirmationDialog open={!!deleting} title="Delete activity?" description="This removes the activity from the current project plan." confirmLabel="Delete activity" danger onClose={()=>setDeleting(undefined)} onConfirm={async()=>{if(deleting)await remove.mutateAsync(deleting);setDeleting(undefined)}}/></>}
