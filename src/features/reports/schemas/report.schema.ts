import { z } from "zod";export const reportSchema=z.object({reportNumber:z.string().trim().min(1),title:z.string().trim().min(2),reportType:z.enum(["daily-progress","weekly-progress","monthly-progress","cost","quality","safety","procurement","executive"]),status:z.enum(["draft","generated","issued","superseded"]),periodStart:z.string().min(1),periodEnd:z.string().min(1),dataDate:z.string().min(1),preparedBy:z.string().trim().min(2),issuedDate:z.string().optional(),revision:z.string().trim().min(1),summary:z.string().optional(),fileName:z.string().optional(),remarks:z.string().optional()}).refine(x=>x.periodEnd>=x.periodStart,{message:"Period end must be on or after period start",path:["periodEnd"]});export type ReportFormValues=z.infer<typeof reportSchema>;
