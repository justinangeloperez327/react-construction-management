import { FileCheck2,FileClock,Files,Plus } from "lucide-react";import { useState } from "react";import { useOutletContext } from "react-router-dom";import { PageHeader } from "@/components/layout";import { Button,ConfirmationDialog,Dialog,ErrorState,MetricCard,Skeleton } from "@/components/ui";import type { Project } from "@/features/projects/types/project";import { ReportForm } from "@/features/reports/components/ReportForm";import { ReportsTable } from "@/features/reports/components/ReportsTable";import { useCreateReport,useDeleteReport,useReports } from "@/features/reports/hooks/useReports";export function ReportsPage(){const {project}=useOutletContext<{project:Project}>();const query=useReports(project.id),create=useCreateReport(project.id),remove=useDeleteReport(project.id);const [adding,setAdding]=useState(false),[deleting,setDeleting]=useState<string>();if(query.isLoading)return <div className="table-loading"><Skeleton/><Skeleton/><Skeleton/></div>;if(query.isError)return <ErrorState title="Unable to load reports" description="The project report register could not be retrieved." onRetry={()=>void query.refetch()}/>;const data=query.data??[];const issued=data.filter(x=>x.status==="issued"),draft=data.filter(x=>["draft","generated"].includes(x.status));return <><PageHeader eyebrow={project.projectNumber} title="Reports" description="Manage controlled project reporting periods, revisions, issue status and report records." actions={<Button onClick={()=>setAdding(true)}><Plus size={16}/>New report</Button>}/><section className="metrics"><MetricCard label="Reports" value={data.length} detail="Controlled report register" icon={<Files size={19}/>}/><MetricCard label="Draft / generated" value={draft.length} detail="Not yet issued" icon={<FileClock size={19}/>}/><MetricCard label="Issued" value={issued.length} detail="Issued reports" icon={<FileCheck2 size={19}/>}/><MetricCard label="Monthly reports" value={data.filter(x=>x.reportType==="monthly-progress").length} detail="Monthly progress series"/></section><section className="card"><ReportsTable items={data} onDelete={setDeleting}/></section><Dialog open={adding} title="New report" description="Register the report period and control metadata. Automated report generation will consume project data rather than duplicate it." onClose={()=>setAdding(false)}><ReportForm onCancel={()=>setAdding(false)} onSubmit={async values=>{await create.mutateAsync({...values,projectId:project.id});setAdding(false)}}/></Dialog><ConfirmationDialog open={!!deleting} title="Delete report?" description="This removes the report metadata from the project report register." confirmLabel="Delete report" danger onClose={()=>setDeleting(undefined)} onConfirm={async()=>{if(deleting)await remove.mutateAsync(deleting);setDeleting(undefined)}}/></>}