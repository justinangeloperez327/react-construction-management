import { Trash2 } from "lucide-react";import type { ColumnDef } from "@tanstack/react-table";import { DataTable } from "@/components/data-table";import { Button,StatusBadge } from "@/components/ui";import { getStatusTone } from "@/design/status";import type { ProjectReport } from "@/features/reports/types/report";import { humanize } from "@/shared/utils";export function ReportsTable({items,onDelete}:{items:ProjectReport[];onDelete:(id:string)=>void}){const columns:ColumnDef<ProjectReport,unknown>[]=[{accessorKey:"reportNumber",header:"Report",cell:({row})=><div><strong>{row.original.reportNumber}</strong><div className="muted">{row.original.title}</div></div>},{accessorKey:"reportType",header:"Type",cell:({row})=>humanize(row.original.reportType)},{accessorKey:"revision",header:"Rev."},{id:"period",header:"Reporting period",cell:({row})=>row.original.periodStart+" → "+row.original.periodEnd},{accessorKey:"dataDate",header:"Data date"},{accessorKey:"preparedBy",header:"Prepared by"},{accessorKey:"status",header:"Status",cell:({row})=><StatusBadge tone={getStatusTone(row.original.status)}>{humanize(row.original.status)}</StatusBadge>},{id:"actions",header:"",enableSorting:false,cell:({row})=><Button variant="ghost" aria-label={"Delete "+row.original.reportNumber} onClick={()=>onDelete(row.original.id)}><Trash2 size={15}/></Button>}];return <DataTable data={items} columns={columns} searchPlaceholder="Search project reports..." emptyTitle="No reports" emptyDescription="Register the first project report."/>}