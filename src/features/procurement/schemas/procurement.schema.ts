import { z } from "zod";export const procurementSchema=z.object({requestNumber:z.string().trim().min(1,"Request number is required"),title:z.string().trim().min(2,"Title is required"),category:z.string().trim().min(2,"Category is required"),materialId:z.string().optional(),supplier:z.string().optional(),quantity:z.coerce.number().positive(),unit:z.string().trim().min(1,"Unit is required"),estimatedValue:z.coerce.number().min(0),currency:z.string().trim().min(3).max(3),requiredDate:z.string().min(1,"Required date is required"),status:z.enum(["draft","requested","rfq","evaluation","approved","ordered","partially-delivered","delivered","cancelled"]),priority:z.enum(["normal","high","critical"]),purchaseOrderNumber:z.string().optional(),orderedDate:z.string().optional(),deliveredQuantity:z.coerce.number().min(0),remarks:z.string().optional()});export type ProcurementFormValues=z.infer<typeof procurementSchema>;
