import type { ReactNode } from "react";
export function FormField({id,label,description,error,required,children}:{id:string;label:string;description?:string;error?:string;required?:boolean;children:ReactNode}){const helpId=description?`${id}-description`:undefined;const errorId=error?`${id}-error`:undefined;return <div className="form-field"><label htmlFor={id}>{label}{required&&<span aria-hidden="true"> *</span>}</label>{description&&<span id={helpId} className="field-description">{description}</span>}<div>{children}</div>{error&&<span id={errorId} className="field-error" role="alert">{error}</span>}</div>}
